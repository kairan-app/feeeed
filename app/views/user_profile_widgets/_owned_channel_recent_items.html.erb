<% items = user.recent_items_from_owned_channels %>
<% if items.exists? %>
<section class="mb-12">
  <h3 class="text-xl font-bold mb-4 pb-2 border-b border-gray-200 text-center">Latest Publishes</h3>
  <%= render(partial: "items/cards", locals: { items: items, with_channel: true }) %>
</section>
<% end %>
