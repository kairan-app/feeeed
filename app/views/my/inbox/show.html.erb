<section>
  <h2 class="text-2xl font-bold mt-6 mb-4">Inbox</h2>
  <%= paginate(@items) %>
  <%= render(partial: "items/cards", locals: { items: @items, with_channel: true }) %>
  <%= paginate(@items) %>
</section>
