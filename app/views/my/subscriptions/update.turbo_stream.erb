<%= turbo_stream.replace @subscription do %>
  <%= render "subscription", subscription: @subscription, subscription_tags: @subscription_tags %>
<% end %>

<%= turbo_stream.update "subscription-tags-list" do %>
  <%= render "subscription_tags_list", subscription_tags: @subscription_tags.reload %>
<% end %>
