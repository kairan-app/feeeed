<section>
  <h2>My Profile</h2>
  <%= form_tag(my_profile_path, method: :put) do |form| %>
    <div class="setting-list">
      <p>Username</p>
      <%= text_field_tag(:name, @user.name) %>
      <div>
        <%= submit_tag("Update") %>
      </div>
    </div>
  <% end %>
</section>
