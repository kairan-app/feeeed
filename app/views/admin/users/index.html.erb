<div class="max-w-6xl mx-auto py-8 px-4">
  <nav class="text-sm text-gray-500 mb-6">
    <%= link_to "Admin", admin_path, class: "hover:underline" %> / <span class="text-gray-800 font-medium">Users</span>
  </nav>

  <div class="bg-white rounded-lg shadow-md p-6">
    <div class="overflow-x-auto">
      <table class="min-w-full table-auto">
        <thead>
          <tr class="bg-gray-50">
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ID</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Avatar</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Subscriptions</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Pawprints</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Last Event</th>
            <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Created at</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @users.each do |user| %>
            <tr>
              <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-500">
                <%= user.id %>
              </td>
              <td class="px-4 py-2 whitespace-nowrap">
                <%= image_tag user.avatar_url(variant: :small), class: "h-8 w-8 rounded-full", alt: user.name %>
              </td>
              <td class="px-4 py-2 whitespace-nowrap">
                <%= link_to user.name, user_path(user_name: user.name), class: "text-blue-600 hover:underline" %>
              </td>
              <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-500">
                <%= user.email %>
              </td>
              <td class="px-4 py-2 text-sm text-gray-500">
                <div class="flex flex-wrap gap-1">
                  <span class="inline-block px-2 py-0.5 bg-blue-100 text-blue-700 text-xs rounded">All (<%= user.subscriptions.count %>)</span>
                  <% user.subscription_tags.each do |tag| %>
                    <span class="inline-block px-2 py-0.5 bg-gray-100 text-gray-700 text-xs rounded"><%= tag.name %> (<%= tag.subscription_taggings.size %>)</span>
                  <% end %>
                </div>
              </td>
              <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-500">
                <%= user.pawprints.count %>
              </td>
              <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-500">
                <%= user.last_event_at&.in_time_zone&.strftime("%Y-%m-%d %H:%M") || "-" %>
              </td>
              <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-500">
                <%= user.created_at.strftime("%Y-%m-%d %H:%M") %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
