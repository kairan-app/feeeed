<section>
  <%= render(partial: "shared/pawprints_header", locals: { title: "All Pawprints" }) %>
  <%= paginate(@pawprints) %>
  <%= render(partial: "pawprints/lines", locals: { pawprints: @pawprints }) %>
  <%= paginate(@pawprints) %>
</section>
