<!DOCTYPE html>
<html>
  <head>
    <title><%= [with_environment_prefix(@title), "rururu"].compact.join(" - ") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% if @noindex %>
    <meta name="robots" content="noindex">
    <% end %>
    <%= favicon_link_tag "square.png" %>
    <meta name="description" content="みんなの登録フィードや感想メモのシェアができる新しいフィードリーダー">
    <meta property="og:title" content="<%= [@title, 'rururu'].compact.join(' - ') %>">
    <meta property="og:description" content="みんなの登録フィードや感想メモのシェアができる新しいフィードリーダー">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:site_name" content="rururu">
    <meta property="og:image" content="<%= image_url('banner.jpg') %>">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,300,0,0" />
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#ecf2fa] font-sans break-words">
    <%# Desktop: Sidebar %>
    <%= render(partial: "layouts/sidebar") %>

    <%# Mobile: Header %>
    <%= render(partial: "layouts/mobile_header") %>

    <%# Main content %>
    <div class="min-h-screen pt-14 pb-16 md:pt-0 md:pb-0 md:ml-60">
      <div class="w-full max-w-[1440px] mx-auto px-5 pb-6">
        <%= render(partial: "layouts/flash_messages") %>
        <%= yield %>
      </div>
    </div>

    <%# Channel registration FAB %>
    <%= render(partial: "channels/preview/form") %>

    <%# Mobile: Bottom tab + Bottom sheet (single Stimulus controller scope) %>
    <div class="md:hidden" data-controller="bottom-sheet">
      <%= render(partial: "layouts/mobile_bottom_tab") %>
      <%= render(partial: "layouts/bottom_sheet") %>
    </div>
  </body>
</html>
