<nav>
  <ul>
    <li><%= link_to("All Channels", channels_path) %></li>
    <li><%= link_to("All Items", items_path) %></li>

    <% if logged_in? %>
    <li><%= link_to("My Inbox", inbox_path) %></li>
    <li><%= link_to("My Profile", user_path(user_name: current_user.name)) %></li>
    <% end %>
  </ul>
</nav>
